{
    "name": "Nextjs",
    "dockerComposeFile": "compose.yml",
    "service": "app",
    "workspaceFolder": "/workspace",
    "customizations": {
        "vscode": {
            "extensions": [
                //ここに拡張機能を追加
            ],
            "settings": {
                //ここに設定を追加
            }
        }
    },
    "postAttachCommand": "sh ./.devcontainer/init.sh",
    "remoteUser": "node",
    "remoteEnv": {
        "LOCAL_WORKSPACE_FOLDER": "${localWorkspaceFolder}"
    }
}